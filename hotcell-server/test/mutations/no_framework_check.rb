# Letting an operation drag an application framework, and its configuration, into the sandbox.
require "hot_cell/server"
module HotCell; class Supervisor; private def verify_no_framework!; end; end; end
